import { useQuery } from '@tanstack/react-query';
import React from 'react';

const MyProducts = () => {
	const {} = useQuery({});

	return (
		<div>
			<h1>My products</h1>
		</div>
	);
};

export default MyProducts;
